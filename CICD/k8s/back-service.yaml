apiVersion: v1
kind: Service
metadata:
  name: stockit-be
  namespace: default
  labels:
    app: stockit-be
spec:
  type: LoadBalancer
  selector:
    app: stockit-be
    color: blue
  ports:
    - name: http
      port: 8080
      targetPort: 8080
